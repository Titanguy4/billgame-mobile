import SwiftUI

struct LoginView: View {
    var body: some View {
        VStack {
            Text("Page de connexion")
                .font(.largeTitle)
                .bold()
        }
        .navigationTitle("Connexion")
    }
}
